"""Research code: corpus construction, labelling, and ranker evaluation.

Nothing here ships in the wheel. These modules build training corpora and
measure rankers; they import from ``beatscope``, never the other way around, so
the product installs and runs without them.
"""
